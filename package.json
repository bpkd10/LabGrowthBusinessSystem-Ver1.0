{
  "name": "business-growth-command-center",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "check": "node --check app/app.js && npm run build"
  }
}
