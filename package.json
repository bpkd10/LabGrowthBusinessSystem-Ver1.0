{
  "name": "business-growth-command-center",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "check:assets": "node scripts/check-assets.mjs",
    "check:ui": "node scripts/check-ui-contract.mjs",
    "check:import": "node scripts/check-data-import.mjs",
    "check:fixtures": "node scripts/check-business-fixtures.mjs",
    "check:workflows": "node scripts/check-business-workflows.mjs",
    "check:model": "node scripts/check-app-model.mjs",
    "check:insights": "node scripts/check-business-insights.mjs",
    "check:report": "node scripts/check-report-export.mjs",
    "check:syntax": "node --check app/app.js && node --check app/business-config.js && node --check app/state-model.js && node --check app/business-workflows.js && node --check app/data-import.js && node --check app/ai-provider.js && node --check app/business-insights.js && node --check app/report-export.js && node --check scripts/*.mjs",
    "precheck:smoke": "npm run build",
    "check:smoke": "node scripts/smoke-build.mjs",
    "check:headers": "node scripts/check-security-headers.mjs",
    "check:deploy-target": "node scripts/check-deploy-target.mjs",
    "check": "npm run check:assets && npm run check:ui && npm run check:import && npm run check:fixtures && npm run check:workflows && npm run check:model && npm run check:insights && npm run check:report && npm run check:syntax && npm run check:smoke && npm run check:headers && npm run check:deploy-target",
    "deploy": "echo '=== manual-only: ต้องรันด้วยมือโดยเจ้าของระบบเท่านั้น ห้าม CI/agent เรียกสคริปต์นี้ — จะเผยแพร่ URL สาธารณะถาวรและถอนคืนไม่ได้ ทำรายการตรวจก่อน deploy ใน docs/adr-001-byok-and-public-link.md ข้อ 8 ระยะ 3 ให้ครบก่อนเสมอ ===' && npm run check && npx wrangler deploy"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "mammoth": "^1.12.0",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  }
}
